---
import DocLayout from '../layouts/DocLayout.astro';
---

<DocLayout title="Manuel d’Utilisation Technique">

  <div class="card" id="infos-document">
    <h2 class="section-title">Informations du document</h2>
    <table role="grid" aria-label="Métadonnées du manuel">
      <thead><tr><th scope="col">Élément</th><th scope="col">Valeur</th></tr></thead>
      <tbody>
        <tr><td><strong>Numéro de document</strong></td><td>MUT-INKSPOT-2026-001</td></tr>
        <tr><td><strong>Titre</strong></td><td>INKSPOT – Manuel d’Utilisation Technique</td></tr>
        <tr><td><strong>Version</strong></td><td>1.1</td></tr>
        <tr><td><strong>Date</strong></td><td>28 février 2026</td></tr>
        <tr><td><strong>Contenu</strong></td><td>Chapitres 1–6, annexes A–H, référence technique</td></tr>
        <tr><td><strong>Référentiel (validation Master)</strong></td><td>YNOV 2024 – Critères CO2.4 (tests &amp; couverture), CO2.5 (exigences techniques, sécurité, accessibilité, évolutivité)</td></tr>
        <tr><td><strong>Plateforme</strong></td><td>Web Full Stack &amp; Mobile First</td></tr>
        <tr><td><strong>Technologies</strong></td><td>Next.js, TypeScript, Railway, PostgreSQL, Prisma</td></tr>
        <tr><td><strong>Rédacteur / Responsable</strong></td><td>Document rédigé par l’équipe projet INKSPOT ; rédacteur : DESPRES Antoine</td></tr>
      </tbody>
    </table>
    <p><strong>Historique des versions</strong></p>
    <table role="grid" aria-label="Historique des versions du manuel">
      <thead>
        <tr>
          <th scope="col">Version</th>
          <th scope="col">Date</th>
          <th scope="col">Modifications</th>
          <th scope="col">Auteur</th>
        </tr>
      </thead>
      <tbody>
        <tr><td>1.0</td><td>Mars 2026</td><td>Version initiale — stack, architecture, flux, API, tests, sécurité, accessibilité, annexes A–H</td><td>DESPRES Antoine</td></tr>
        <tr><td>1.1</td><td>28 février 2026</td><td>Ajout OWASP Top 10, RGAA 4.1, manuel de mise à jour (C2.4.1) — version finale de rendu</td><td>DESPRES Antoine</td></tr>
      </tbody>
    </table>
  </div>

  <div class="card" id="conformite-referentiel-mut">
    <h2 class="section-title">Conformité référentiel (validation Master)</h2>
    <p>Ce manuel couvre les critères du bloc 2 (référentiel YNOV 2024) attendus pour le livrable « Manuel d’utilisation technique » :</p>
    <table role="grid" aria-label="Correspondance critères Master et sections MUT">
      <thead><tr><th scope="col">Critère</th><th scope="col">Attendu</th><th scope="col">Section(s) de ce manuel</th></tr></thead>
      <tbody>
        <tr>
          <td><strong>CO2.4</strong></td>
          <td>Harnais de tests, couverture, prévention des régressions</td>
          <td><a href="#tests">Tests &amp; couverture (CO2.4)</a> — Jest, Playwright, suites unitaires/E2E, coverage, modules critiques (Auth, Réservations, Paiement, Notifications). Méthodologie de test (section 5.b).</td>
        </tr>
        <tr>
          <td><strong>CO2.5</strong></td>
          <td>Évolutivité, sécurité, accessibilité, exigences techniques</td>
          <td><a href="#exigences">Exigences techniques &amp; fonctionnelles (CO2.5)</a>, <a href="#securite">Sécurité</a> (webhooks Stripe, validation zod, rate-limit, secrets, auth), <a href="#accessibilite">Accessibilité (CO2.5)</a> (checklist ARIA, focus, clavier, contrastes), <a href="#evolutivite">Évolutivité &amp; maintenabilité</a> (Prisma, Redis, modules).</td>
        </tr>
      </tbody>
    </table>
    <p>Le livrable « Cahier de recette » (critère CO2.7) est traité dans le document dédié : <a href="/cahier-recette">Cahier de Recette</a>.</p>
  </div>

  <div class="card" id="resume-executif">
    <h2 class="section-title">Résumé exécutif</h2>
    <p>Contenu couvert :</p>
    <ul class="list-tight">
      <li><strong>Document (chapitres 1–6)</strong> : Introduction (présentation INKSPOT, vision, pour qui, approche technique, philosophie), stack technique, dépendances (scripts, principales, procédure de gestion), système de supervision (métriques, statuts, indicateurs), maintenance et évolution (CI/CD, méthodologie de test, méthodologie GitHub), modèle économique et fonctionnalités (commissions 3,5 % + 0,50 €, paiements Stripe), conclusion.</li>
      <li><strong>Annexes A à H</strong> : architecture stack, flux auth, monitoring, gestion des dépendances, CI/CD, supervision détaillée, paiements &amp; commissions, fonctionnalités &amp; workflows.</li>
      <li><strong>Référence technique rapide</strong> : architecture &amp; modules, flux de données, API principales, rôles &amp; permissions, parcours fonctionnels, exigences, données &amp; intégrité, configuration, tests &amp; couverture (CO2.4), sécurité, accessibilité (CO2.5), évolutivité, monitoring, exploitation, dépannage.</li>
    </ul>
  </div>

  <div class="card" id="prerequis-mut">
    <h2 class="section-title">Prérequis</h2>
    <p>Prérequis pour l’application INKSPOT :</p>
    <ul class="list-tight">
      <li><strong>Environnement</strong> : Node.js 22 (fichier <code>.nvmrc</code>), npm 10+</li>
      <li><strong>Outils</strong> : Git, éditeur de code, terminal</li>
      <li><strong>Comptes / services</strong> : GitHub (dépôt projet), Railway (hébergement), Stripe (paiements), optionnellement Redis, S3-compatible (assets)</li>
      <li><strong>Connaissances</strong> : bases Next.js/TypeScript, Prisma, REST API</li>
    </ul>
  </div>

  <div class="card" id="demarrage-rapide">
    <h2 class="section-title">Démarrage rapide</h2>
    <p>Pour lancer le projet en local (développement) :</p>
    <ol class="list-tight">
      <li><strong>Cloner le dépôt</strong> : <code>git clone https://github.com/antoine13330/INKSPOT-5z</code> puis <code>cd INKSPOT-5z</code></li>
      <li><strong>Version Node</strong> : <code>nvm use</code> (ou installer Node 22 selon <code>.nvmrc</code>)</li>
      <li><strong>Installer les dépendances</strong> : <code>npm install</code></li>
      <li><strong>Variables d’environnement</strong> : copier <code>railway.env.example</code> vers <code>.env.local</code> et renseigner les valeurs (voir section <a href="#variables-env-mut">Variables d’environnement</a> et <a href="/deploiement-railway">Manuel Déploiement Railway</a>)</li>
      <li><strong>Base de données</strong> : <code>npm run db:setup</code> (ou <code>npx prisma migrate deploy</code> + <code>npm run db:seed</code>)</li>
      <li><strong>Lancer l’application</strong> : <code>npm run dev</code> — l’app est disponible sur <code>http://localhost:3000</code></li>
    </ol>
    <p>Vérification rapide : <code>npm run lint</code>, <code>npm run test</code> (tests unitaires), <code>npm run test:e2e</code> (E2E).</p>
    <div class="callout" style="margin-top:1rem;">
      <strong>Checklist première mise en route</strong>
      <ul class="list-tight" style="margin-bottom:0;">
        <li>Node 22 (<code>nvm use</code> ou selon <code>.nvmrc</code>)</li>
        <li><code>.env.local</code> renseigné (voir <a href="#variables-env-mut">Variables d’environnement</a>)</li>
        <li>Base de données migrée et seedée (<code>npm run db:setup</code>)</li>
        <li><code>npm run dev</code> lance l’app sans erreur</li>
        <li><code>npm run lint</code> et <code>npm run test</code> passent (ou à corriger)</li>
      </ul>
    </div>
  </div>

  <div class="card" id="variables-env-mut">
    <h2 class="section-title">Variables d’environnement</h2>
    <p>Les variables suivantes sont nécessaires au fonctionnement de l’application. Pour la liste complète et le détail (Requis / Usage), voir le <a href="/deploiement-railway">Manuel de Déploiement Railway</a>.</p>
    <table role="grid" aria-label="Variables d’environnement principales">
      <thead><tr><th scope="col">Variable</th><th scope="col">Usage</th></tr></thead>
      <tbody>
        <tr><td><code>DATABASE_URL</code></td><td>Connexion PostgreSQL</td></tr>
        <tr><td><code>NEXTAUTH_URL</code></td><td>URL publique de l’app (ex. http://localhost:3000)</td></tr>
        <tr><td><code>NEXTAUTH_SECRET</code></td><td>Signature des sessions</td></tr>
        <tr><td><code>STRIPE_SECRET_KEY</code>, <code>STRIPE_PUBLISHABLE_KEY</code>, <code>STRIPE_WEBHOOK_SECRET</code></td><td>Paiements Stripe</td></tr>
        <tr><td><code>AWS_ACCESS_KEY_ID</code>, <code>AWS_SECRET_ACCESS_KEY</code>, <code>AWS_S3_BUCKET</code>, <code>AWS_REGION</code></td><td>Stockage S3 (médias)</td></tr>
        <tr><td><code>VAPID_PUBLIC_KEY</code>, <code>VAPID_PRIVATE_KEY</code></td><td>Notifications push (optionnel)</td></tr>
        <tr><td><code>REDIS_URL</code></td><td>Cache / sessions (optionnel en dev)</td></tr>
      </tbody>
    </table>
    <p>Validation : <code>npm run env:check</code> (si disponible) ou vérifier la présence des clés dans <code>.env.local</code>.</p>
  </div>

  <div class="card" id="structure-projet-mut">
    <h2 class="section-title">Structure du projet</h2>
    <p>Organisation des répertoires principaux :</p>
    <ul class="list-tight">
      <li><code>app/</code> — Pages et routes Next.js (App Router) ; <code>app/api/**</code> pour les API</li>
      <li><code>components/</code> — Composants React réutilisables (UI, layout, métier)</li>
      <li><code>lib/</code> — Utilitaires, services, configuration (Stripe, validation, websocket)</li>
      <li><code>prisma/</code> — Schéma Prisma (<code>schema.prisma</code>), seed, migrations</li>
      <li><code>hooks/</code> — Hooks React personnalisés</li>
      <li><code>middleware/</code> — Middleware Next.js (sécurité, monitoring)</li>
      <li><code>e2e/</code> — Tests end-to-end Playwright</li>
      <li><code>__tests__/</code> — Tests unitaires et d’intégration Jest</li>
      <li><code>docs/</code> — Documentation technique (CI/CD, déploiement, env)</li>
      <li><code>scripts/</code> — Scripts utilitaires (déploiement, vérification)</li>
    </ul>
  </div>

  <div class="card toc">
    <strong>Sommaire</strong>
    <p class="toc-group"><strong>Document</strong></p>
    <ul class="list-tight">
      <li><a href="#infos-document">Informations du document</a></li>
      <li><a href="#conformite-referentiel-mut">Conformité référentiel (validation Master)</a></li>
      <li><a href="#resume-executif">Résumé exécutif</a></li>
      <li><a href="#prerequis-mut">Prérequis</a></li>
      <li><a href="#demarrage-rapide">Démarrage rapide</a></li>
      <li><a href="#variables-env-mut">Variables d’environnement</a></li>
      <li><a href="#structure-projet-mut">Structure du projet</a></li>
      <li><a href="#introduction">1. Introduction</a></li>
      <li><a href="#stack">2. Stack technique</a></li>
      <li><a href="#dependances">3. Dépendances</a></li>
      <li><a href="#supervision">4. Système de supervision</a></li>
      <li><a href="#maintenance">5. Maintenance et évolution</a></li>
      <li><a href="#modele-economique">6. Modèle économique et fonctionnalités</a></li>
    </ul>
    <p class="toc-group"><strong>Référence technique rapide</strong></p>
    <ul class="list-tight">
      <li><a href="#architecture">Architecture &amp; modules</a></li>
      <li><a href="#flux">Flux de données</a></li>
      <li><a href="#api">API principales</a></li>
      <li><a href="#roles">Rôles &amp; permissions</a></li>
      <li><a href="#parcours">Parcours fonctionnels clés</a></li>
      <li><a href="#exigences">Exigences techniques</a></li>
      <li><a href="#donnees">Données &amp; intégrité</a></li>
      <li><a href="#configuration">Configuration &amp; scripts</a></li>
      <li><a href="#tests">Tests &amp; couverture (CO2.4)</a></li>
      <li><a href="#securite">Sécurité (OWASP Top 10)</a></li>
      <li><a href="#accessibilite">Accessibilité (RGAA 4.1)</a></li>
      <li><a href="#mise-a-jour">Manuel de mise à jour (C2.4.1)</a></li>
            <li><a href="#evolutivite">Évolutivité &amp; maintenabilité</a></li>
      <li><a href="#monitoring">Monitoring / Healthchecks</a></li>
      <li><a href="#exploitation">Exploitation &amp; runbook</a></li>
      <li><a href="#depannage">Dépannage rapide</a></li>
    </ul>
    <p class="toc-group"><strong>Annexes &amp; références</strong></p>
    <ul class="list-tight">
      <li><a href="#annexes">Annexes (A–H)</a></li>
      <li><a href="#glossaire-mut">Glossaire</a></li>
      <li><a href="#references-croisees-mut">Références croisées</a></li>
      <li><a href="#table-sections-mut">Table des sections</a></li>
    </ul>
  </div>

  <div class="card" id="introduction">
    <h2 class="section-title">1. Introduction</h2>
    <h3>Présentation d’INKSPOT</h3>
    <p>INKSPOT est bien plus qu’une simple plateforme numérique : c’est un véritable carrefour créatif où l’art du tatouage prend vie dans l’ère digitale. Née de la passion pour l’art corporel et de la volonté de rapprocher les talents des amateurs, INKSPOT révolutionne la façon dont artistes et clients se rencontrent, échangent et collaborent.</p>
    <h3>Vision et Mission</h3>
    <p>Notre mission est de démocratiser l’accès à l’art du tatouage en créant un pont numérique entre artistes talentueux et clients en quête d’expression artistique personnelle. INKSPOT vise à :</p>
    <ul class="list-tight">
      <li>Valoriser le travail des artistes en leur offrant une vitrine professionnelle moderne</li>
      <li>Faciliter la découverte d’artistes et de styles pour les amateurs de tatouage</li>
      <li>Simplifier les échanges entre professionnels et clients</li>
      <li>Créer une communauté bienveillante autour de la passion commune</li>
    </ul>
    <h3>Pour qui ?</h3>
    <ul class="list-tight">
      <li><strong>Pour les Artistes Tatoueurs</strong> : INKSPOT offre aux professionnels du tatouage un espace dédié pour présenter leur portfolio, gérer leurs rendez-vous, échanger avec leur clientèle et développer leur activité dans un environnement sécurisé et moderne.</li>
      <li><strong>Pour les Amateurs d’Art Corporel</strong> : La plateforme permet aux passionnés de découvrir des artistes, explorer différents styles, suivre leurs créateurs favoris, et organiser facilement leurs projets de tatouage.</li>
    </ul>
    <h3>Approche Technique</h3>
    <p>INKSPOT est construite comme une application web moderne et responsive, privilégiant l’expérience mobile pour s’adapter aux habitudes d’utilisation contemporaines. La plateforme est actuellement hébergée en environnement de développement, permettant une évolution continue et l’ajout progressif de nouvelles fonctionnalités basées sur les retours des utilisateurs.</p>
    <h3>Accessibilité Multi-plateforme</h3>
    <p>Conçue en « mobile first », INKSPOT s’adapte parfaitement aux smartphones, tablettes et ordinateurs, garantissant une expérience utilisateur optimale sur tous les supports.</p>
    <h3>Philosophie du Projet</h3>
    <p>INKSPOT s’inscrit dans une démarche de respect et de valorisation de l’art du tatouage. Nous croyons que chaque tatouage raconte une histoire unique et que chaque artiste mérite d’être reconnu pour son talent et sa créativité. La plateforme transcende les barrières géographiques traditionnelles en offrant une vitrine mondiale aux artistes tatoueurs, qu’ils soient émergents ou confirmés, tout en garantissant aux clients une expérience de découverte riche et personnalisée. INKSPOT cultive ainsi une communauté bienveillante où le respect mutuel, la créativité et l’excellence artistique sont au cœur de chaque interaction.</p>
  </div>

  <div class="card" id="stack">
    <h2 class="section-title">2. Stack technique</h2>
    <p>Voici un tableau de la stack technique. Un schéma détaillé est disponible en annexe A.</p>
    <table role="grid" aria-label="Stack technique par catégorie">
      <thead>
        <tr><th scope="col">Catégorie</th><th scope="col">Technologie</th></tr>
      </thead>
      <tbody>
        <tr><td>Frontend</td><td>Next.js (TypeScript) – Interface mobile-first</td></tr>
        <tr><td>Backend/API</td><td>Next.js API Routes, Tailwind CSS, Radix UI / shadcn/ui</td></tr>
        <tr><td>Hébergement/Service</td><td>Railway</td></tr>
        <tr><td>Base de données</td><td>PostgreSQL + Prisma ORM</td></tr>
        <tr><td>Authentification</td><td>NextAuth.js</td></tr>
        <tr><td>Payement</td><td>Stripe</td></tr>
        <tr><td>Cache & Sessions</td><td>Redis</td></tr>
        <tr><td>Monitoring</td><td>Prometheus + Grafana</td></tr>
        <tr><td>Tests</td><td>Jest + Playwright</td></tr>
      </tbody>
    </table>
    <p>Complément technique : Next.js 15 (app router), S3 (assets), Nginx (reverse-proxy), Docker, swagger-jsdoc.</p>
  </div>

  <div class="card" id="dependances">
    <h2 class="section-title">3. Dépendances</h2>
    <p>La partie la plus lourde de l’application sont les dépendances ; actuellement la version de Node est la 22 (écrite dans le fichier <code>.nvmrc</code>).</p>
    <h3>3.a Scripts</h3>
    <p>Les scripts disponibles sont notés dans le <code>package.json</code> :</p>
    <ul class="list-tight">
      <li><code>npm run dev</code> – lance un serveur de développement avec Next.js</li>
      <li><code>npm run build</code> – crée un Build de production avec migrations Prisma</li>
      <li><code>npm run start</code> – lance un serveur de production</li>
      <li><code>npm run test</code> – exécution des tests unitaires Jest</li>
      <li><code>npm run test:e2e</code> – exécution des tests end-to-end Playwright</li>
      <li><code>npm run db:seed</code> – peuple la base de données</li>
      <li><code>npm run db:setup</code> – fait la configuration initiale de la base de données</li>
      <li><code>npm run lint</code> – Vérification du code</li>
      <li><code>npm run analyze</code> – génère un rapport du poids du bundle</li>
    </ul>
    <h3>3.b Dépendances principales</h3>
    <p>Les dépendances principales sont celles de Prisma, NextAuth.js, Stripe, shadcn/ui et le système de monitoring :</p>
    <ul class="list-tight">
      <li><strong>Prisma</strong> : Initialisez avec <code>npx prisma init</code> pour créer un schéma. Les modèles sont dans <code>schema.prisma</code>. Générez le client avec <code>npx prisma generate</code> et appliquez les migrations avec <code>npx prisma migrate</code>. Utilisez le client généré pour effectuer des requêtes CRUD dans vos pages ou API routes.</li>
      <li><strong>NextAuth.js</strong> : solution d’authentification complète pour Next.js (inscription, connexion, sessions, OAuth, JWT).</li>
      <li><strong>Stripe</strong> : plateforme de paiement en ligne (cartes de crédit, abonnements, facturation). Lors de la sortie de ce manuel, les données sont factices donc lors d’un achat personne n’est débité.</li>
      <li><strong>shadcn/ui</strong> : bibliothèque de composants UI réutilisables (Tailwind CSS, Radix UI). Ajoutez des composants avec <code>npx shadcn-ui@latest add button</code> (par exemple).</li>
      <li><strong>Système de monitoring</strong> : Prometheus collecte les métriques, Grafana fournit les dashboards, Redis optimise les performances avec le cache et la gestion des sessions.</li>
    </ul>
    <h3>3.c Procédure détaillée de gestion des dépendances</h3>
    <p>Pour maintenir le projet à jour et sécurisé, il est essentiel de gérer les dépendances de manière prudente. Ce projet utilise peu de dépendances, dispose de tests unitaires et d’un script <code>npm run analyze</code> pour analyser les paquets. Voici une approche simple et sûre pour les mettre à jour :</p>
    <p><strong>⚠ Attention</strong> : Testez toujours les mises à jour dans un environnement de développement avant la production. Effectuez ces étapes dans un environnement de test (ex. branche Git dédiée) pour éviter les disruptions.</p>
    <ol class="list-tight">
      <li><strong>Vérifier les vulnérabilités</strong> : Exécutez <code>npm audit</code> pour lister les vulnérabilités. Si des corrections sont disponibles, appliquez <code>npm audit fix</code> et vérifiez manuellement les impacts sur le code.</li>
      <li><strong>Identifier les dépendances obsolètes</strong> : Lancez <code>npm outdated</code> pour voir les versions actuelles, désirées et les dernières disponibles (respect des contraintes semver du <code>package.json</code>).</li>
      <li><strong>Mettre à jour de manière sûre</strong> : Utilisez <code>npm update</code> pour les mises à jour mineures et de patch compatibles (méthode la plus sûre pour changements mineurs). Pour des mises à jour majeures (ex. 1.x → 2.x), éditez manuellement le <code>package.json</code> pour spécifier la nouvelle version, puis <code>npm install</code> pour mettre à jour le <code>package-lock.json</code>.</li>
      <li><strong>Conseils pour la sécurité</strong> : Mettez à jour régulièrement, mais pas plus d’une dépendance à la fois si possible pour isoler les problèmes. Consultez les changelogs sur npmjs.com avant les mises à jour majeures. Si une dépendance est critique et vulnérable sans mise à jour compatible, envisagez une alternative. Cette approche minimise les risques tout en gardant le projet à jour. Après toute mise à jour, validez la stabilité : lancez les tests unitaires avec <code>npm test</code>, exécutez <code>npm run analyze</code> pour une analyse supplémentaire, testez manuellement l’application pour détecter d’éventuels bugs.</li>
    </ol>
  </div>

  <div class="card" id="supervision">
    <h2 class="section-title">4. Système de supervision</h2>
    <h3>Vue d’ensemble</h3>
    <p>Le système de supervision d’INKSPOT s’appuie sur Prometheus et Grafana pour surveiller en temps réel les performances, la santé et l’activité de l’application. Il collecte automatiquement des métriques couvrant les API, la base de données et les activités métier, afin d’offrir une vision claire et continue de ce qui se passe dans la plateforme. Le cœur du monitoring repose sur un collecteur de métriques personnalisé, basé sur Prometheus, qui instrumente automatiquement l’application et centralise la collecte des données de performance.</p>
    <h3>Les types de métriques implémentés</h3>
    <ul class="list-tight">
      <li><strong>Métriques API</strong> : Nombre total de requêtes par endpoint, durée des requêtes (secondes), taux d’erreurs par code de statut HTTP, méthodes HTTP utilisées (GET, POST, etc.).</li>
      <li><strong>Métriques Base de données</strong> : Nombre de requêtes exécutées par table, temps de réponse des requêtes (secondes), types d’opérations (select, create, update, delete), performance des connexions.</li>
      <li><strong>Métriques métier</strong> : Nombre total d’utilisateurs, nombre total de posts, nombre total de réservations, nombre total de paiements, réservations du jour, revenus du jour, statut des connexions base de données et Redis.</li>
    </ul>
    <p>L’interface d’administration est disponible sur Grafana en production, accessible à partir d’un compte global pour chaque utilisateur.</p>
    <h3>Interprétation des indicateurs</h3>
    <table role="grid" aria-label="Seuils et interprétation des métriques">
      <thead><tr><th scope="col">Métrique</th><th scope="col">Seuils principaux</th><th scope="col">Interprétation rapide</th></tr></thead>
      <tbody>
        <tr><td>Temps de réponse API</td><td>&lt;200 ms • 200–500 ms • &gt;1 s</td><td>Excellent • Bon • À corriger</td></tr>
        <tr><td>Taux d’erreurs (HTTP)</td><td>&lt;1 % • 1–5 % • &gt;5 %</td><td>Normal • À surveiller • Critique</td></tr>
        <tr><td>Temps de réponse DB</td><td>&lt;100 ms • 100 ms–1 s • &gt;1 s</td><td>Rapide • Lente • À corriger</td></tr>
      </tbody>
    </table>
    <p>Les métriques métier aident à suivre l’activité réelle des utilisateurs (inscriptions par jour, utilisateurs actifs simultanés, fréquence d’utilisation des fonctionnalités clés, qualité du contenu, tendances d’engagement). Le système génère automatiquement des alertes lorsque certains seuils ou comportements anormaux sont détectés (taux d’erreurs élevés sur les API, temps de réponse anormalement longs, problèmes de connectivité avec la base de données ou anomalies dans les métriques métier).</p>
    <h3>Tableau des statuts possibles</h3>
    <table role="grid" aria-label="Statuts des rapports de supervision">
      <thead><tr><th scope="col">Statut</th><th scope="col">Définition</th><th scope="col">Exemple d’usage</th></tr></thead>
      <tbody>
        <tr><td>En rapport</td><td>Rapport créé, en attente de prise en charge.</td><td>Signalement initial d’une hausse d’erreurs API.</td></tr>
        <tr><td>En cours</td><td>Analyse ou correction en cours.</td><td>Un admin enquête sur des requêtes DB lentes.</td></tr>
        <tr><td>Résolu</td><td>Problème identifié et corrigé.</td><td>Patch déployé, les temps de réponse reviennent à la normale.</td></tr>
        <tr><td>Fermé</td><td>Vérification post-correction effectuée, dossier clôturé.</td><td>Rapport traité et archivé après validation.</td></tr>
      </tbody>
    </table>
    <h3>Niveaux de priorité</h3>
    <table role="grid" aria-label="Niveaux de priorité des alertes">
      <thead><tr><th scope="col">Priorité</th><th scope="col">Définition</th><th scope="col">Délai de traitement conseillé</th></tr></thead>
      <tbody>
        <tr><td>Faible</td><td>Impact minimal sur le service.</td><td>Planifié dans le prochain cycle de maintenance.</td></tr>
        <tr><td>Moyenne</td><td>Impact modéré nécessitant une attention.</td><td>Sous 72 heures.</td></tr>
        <tr><td>Élevée</td><td>Impact significatif sur les utilisateurs.</td><td>Sous 24 heures.</td></tr>
        <tr><td>Urgente</td><td>Problème critique nécessitant une intervention immédiate.</td><td>Immédiat (heures).</td></tr>
      </tbody>
    </table>
    <p><strong>💡 Bonnes pratiques</strong> : Un entretien régulier facilite la stabilité et la performance. Chaque semaine, revoir les rapports non résolus et analyser les tendances de performance afin d’anticiper les problèmes. Une fois par mois, effectuer une révision complète du système, mettre à jour les seuils d’alerte en fonction de l’évolution de la charge et planifier les améliorations à mettre en œuvre.</p>
  </div>

  <div class="card" id="maintenance">
    <h2 class="section-title">5. Maintenance et évolution</h2>
    <h3>5.a Déploiement continu</h3>
    <p>INKSPOT dispose d’un système de CI/CD appuyé sur GitHub Actions et Railway. À chaque push ou Pull Request vers les branches <code>dev</code> ou <code>main</code>, une pipeline sous Node.js 22 exécute automatiquement les tests unitaires et d’intégration. Pour la branche de production (<code>main</code>), une étape supplémentaire analyse le bundle afin de contrôler la taille et les performances. En parallèle, le journal des changements (<code>CHANGELOG.md</code>) est mis à jour automatiquement par un script dédié, puis poussé sur le dépôt pour assurer une traçabilité claire des évolutions. Le déploiement est assuré par Railway, sans action manuelle. Chaque Pull Request déclenche la création d’un environnement de Preview avec une URL dédiée pour la validation visuelle et fonctionnelle. La branche <code>dev</code> sert d’environnement de préproduction (recette). Actuellement, la production est temporairement hébergée sur l’environnement de développement pour permettre une évolution rapide et continue de la plateforme. Les en-têtes de sécurité sont appliqués côté Railway pour renforcer la protection dans le navigateur. En pratique, le flux de travail est le suivant : développer sur une branche dédiée, ouvrir une Pull Request, laisser la CI exécuter les vérifications, valider la Preview Railway, puis fusionner. La recette s’effectue sur <code>dev</code>. Aucune commande de déploiement n’est requise : la chaîne CI/CD garantit des livraisons rapides, fiables et traçables. Voir annexe diagramme de flux.</p>
    <h3>5.b Méthodologie de test</h3>
    <p>Pour assurer une maintenance durable, les tests unitaires et end-to-end font partie intégrante du code et du flux CI/CD. Ils servent à verrouiller le comportement attendu, faciliter les refactorings et réduire le coût des régressions. <strong>Ce qu’il faut tester en priorité</strong> : règles métiers, transformations de données, formats d’entrées/sorties, gestion des erreurs, contrats de composants (rendu conditionnel, accessibilité de base, interactions critiques). Les snapshots doivent rester ciblés et stables ; éviter les snapshots massifs et volatils. <strong>Critères de qualité</strong> : viser une couverture significative et pertinente (branches et chemins critiques) sans transformer la couverture en objectif purement quantitatif. Un test doit échouer pour une bonne raison et réussir pour la bonne raison. <strong>Rapidité et fiabilité</strong> : un test unitaire doit s’exécuter en millisecondes et donner toujours le même résultat. Éliminer les tests « flaky » avant fusion ; un test instable nuit à toute la chaîne. <strong>Variables d’environnement et flags</strong> : documenter les valeurs par défaut utilisées en test, et couvrir le comportement sous flags ou modes (ex. préproduction/production) lorsque cela affecte la logique. <strong>Revue et dette technique</strong> : toute Pull Request modifiant la logique fonctionnelle doit ajouter ou mettre à jour des tests pertinents. Les tests fragiles ou redondants sont refactorés régulièrement pour rester utiles et lisibles. Cette discipline de tests, combinée au pipeline GitHub Actions et aux déploiements Railway, maintient un cycle de livraison maîtrisé : on protège la qualité au plus près du code, on détecte tôt les régressions, et on livre en confiance sur <code>develop</code> (préproduction) puis sur <code>main</code> (production).</p>
    <h3>5.c Méthodologie GitHub</h3>
    <p>INKSPOT suit un flux de travail simple et reproductible qui part d’un besoin documenté et se termine par une fonctionnalité livrée et tracée. L’objectif est d’assurer clarté, traçabilité et vitesse, sans surcharge pour les développeurs. Voici les étapes :</p>
    <ol class="list-tight">
      <li><strong>Création et cadrage du ticket</strong> : Lorsqu’un besoin émerge (évolution, correction, dette technique), il est créé dans GitHub Projects sous la forme d’un ticket. Le ticket décrit le contexte, l’objectif, les critères d’acceptation et, si nécessaire, les risques connus et impacts. Cette documentation rend explicites les attentes et sert de référence pendant tout le cycle.</li>
      <li><strong>Classification par tags</strong> : Le ticket reçoit un ou plusieurs tags (ex. feature, bug, tech-debt, perf, a11y, sécurité) ainsi qu’une priorité. Ces tags facilitent la planification, la recherche et le suivi du travail, et permettent d’appliquer des règles de qualité adaptées (revue renforcée pour la sécurité, seuils spécifiques pour la performance, etc.).</li>
      <li><strong>Création automatique de la branche à partir du ticket</strong> : Dès que le ticket est prêt à être traité, une branche dédiée est créée automatiquement à partir des informations du ticket. La convention de nommage reste cohérente et traçable (par exemple, préfixe lié au type et identifiant du ticket). Cette automatisation réduit les erreurs, applique les conventions d’équipe et garantit le lien explicite entre code et besoin.</li>
      <li><strong>Développement et synchronisation continue</strong> : Le développement se fait sur cette branche dédiée. Les commits sont atomiques et mentionnent l’identifiant du ticket pour conserver la traçabilité. À chaque push, la CI exécute les tests unitaires et les autres contrôles qualité. En parallèle, Railway génère des Previews pour valider visuellement les changements au fil de l’eau.</li>
      <li><strong>Pull Request et liaison au ticket</strong> : Une fois la tâche aboutie, une Pull Request est ouverte et liée au ticket. La description rappelle brièvement l’objectif, les changements principaux, les critères d’acceptation et, si pertinent, les impacts techniques. Cette liaison garantit que le ticket sera automatiquement mis à jour lorsque la PR sera fusionnée.</li>
      <li><strong>Script de déploiement continue</strong> : La Preview Railway associée à la PR est vérifiée par l’équipe (et, si nécessaire, par la personne qui a porté le besoin). Cette étape confirme que le résultat correspond aux critères d’acceptation et qu’aucune régression visuelle majeure n’a été introduite.</li>
      <li><strong>Fusion, nettoyage et passage du ticket en « Fait »</strong> : Après approbation et checks verts, la PR est fusionnée vers la branche cible (<code>dev</code> pour la préproduction, <code>main</code> pour la production selon le cas). La branche de travail est supprimée afin d’éviter l’encombrement et les confusions. Le ticket passe automatiquement à l’état « Fait », ce qui clôt le cycle et met à jour la visibilité dans GitHub Projects.</li>
      <li><strong>Traçabilité et retour d’expérience</strong> : Le changelog est mis à jour automatiquement, rendant visibles les évolutions livrées. En cas d’incident ou d’écart, un court retour d’expérience est noté sur le ticket ou dans la documentation associée, pour capitaliser et améliorer en continu la méthode. Ce cadre, du ticket initial jusqu’à la suppression de la branche et la mise en « Fait », garantit un flux prévisible, des déploiements sûrs, et une traçabilité complète entre besoin, code, tests et livraison. Il s’intègre naturellement au pipeline CI/CD et aux Previews Railway, tout en laissant de la place à l’amélioration continue.</li>
    </ol>
    <p><strong>Implémentation et bonnes pratiques des tests</strong> : Portée des tests : chaque module ou composant doit avoir des tests qui couvrent ses règles métiers, ses chemins d’erreur et ses contrats publics (fonctions exportées, composants, utilitaires). Organisation : les tests vivent au plus près du code testé (même répertoire) avec une convention de nommage claire pour être découverts automatiquement par la CI. Un fichier de test par unité logique, des noms de cas lisibles et orientés comportement. Données de test : utiliser des jeux de données simples, explicites et reproductibles. Les « fixtures » et « factories » aident à créer des objets cohérents sans bruit. Éviter les dépendances à des ressources externes ; préférer des doublures. Isolation et mock : isoler l’unité testée en remplaçant les appels aux services externes (HTTP, base de données, file system, SDK tiers) par des mocks/stubs contrôlés. Le but est d’obtenir des tests rapides, déterministes et indépendants du réseau.</p>
  </div>

  <div class="card" id="modele-economique">
    <h2 class="section-title">6. Modèle économique et fonctionnalités</h2>
    <h3>6.a Modèle économique</h3>
    <p>INKSPOT génère ses revenus principalement via un système de commissions sur les réservations effectuées par les utilisateurs. Pour chaque réservation payée via Stripe, INKSPOT prélève une commission de <strong>3,5 % + 50 centimes</strong>. Ce modèle permet à la plateforme de se rémunérer tout en offrant un service gratuit aux utilisateurs. Un système d’abonnement premium est également prévu pour l’avenir, offrant des fonctionnalités avancées aux utilisateurs qui souhaitent bénéficier d’options supplémentaires. <strong>🚀 Évolution future</strong>.</p>
    <h3>6.b Fonctionnalités principales</h3>
    <p>INKSPOT propose une plateforme complète de mise en relation entre artistes tatoueurs et clients.</p>
    <p><strong>Pour tous les utilisateurs</strong></p>
    <ul class="list-tight">
      <li>Création de compte et profil (inscription, authentification, personnalisation du profil)</li>
      <li>Recherche et découverte (recherche d’artistes et de posts, filtres avancés)</li>
      <li>Interactions sociales (abonnement aux artistes, commentaires, likes sur les posts)</li>
      <li>Communication (envoi de messages directs aux artistes)</li>
      <li>Contenu (création et partage de posts avec images et hashtags)</li>
    </ul>
    <p><strong>Pour les artistes (PRO)</strong></p>
    <ul class="list-tight">
      <li>Gestion des réservations (proposition de créneaux, gestion du planning)</li>
      <li>Outils professionnels (tableau de bord avancé, analytics de performance)</li>
      <li>Collaborations (possibilité de collaborer sur des posts)</li>
      <li>Suivi financier (historique des rendez-vous, revenus générés)</li>
      <li>Gestion des disponibilités (définition des créneaux disponibles)</li>
      <li>Évaluations (système de reviews et commentaires)</li>
    </ul>
    <p><strong>Pour les clients</strong></p>
    <ul class="list-tight">
      <li>Réservations (prise de rendez-vous avec les artistes)</li>
      <li>Paiements sécurisés (Stripe avec protection SCA/3‑D Secure)</li>
      <li>Suivi (historique des rendez-vous passés et à venir)</li>
    </ul>
    <h3>6.c Gestion des paiements Stripe</h3>
    <p>Le système de paiement d’INKSPOT est entièrement intégré avec Stripe pour assurer sécurité et fiabilité. Chaque réservation génère automatiquement une transaction Stripe avec calcul automatique des commissions. <strong>Flux de paiement et commissions</strong> : Réservation client (le client sélectionne un créneau et procède au paiement). Paiement Stripe (transaction sécurisée via Stripe Checkout avec SCA/3‑D Secure). Calcul des commissions (INKSPOT prélève automatiquement 3,5 % + 50 centimes). Virement artiste (le montant net est transféré sur le compte de l’artiste). Suivi financier (toutes les transactions sont tracées et consultables). <strong>Synchronisation et webhooks</strong> : Webhooks Stripe (toutes les notifications de paiement sont traitées en temps réel). Idempotence (chaque webhook est traité de manière idempotente pour éviter les doublons). Vérification cryptographique (signature des webhooks vérifiée pour la sécurité). Synchronisation base de données (statut des paiements mis à jour automatiquement).</p>
    <h3>Conclusion</h3>
    <p>INKSPOT s’appuie sur une architecture moderne et cohérente (Next.js/Railway, PostgreSQL/Prisma, Prometheus → Grafana) et un cycle de livraison maîtrisé (CI/CD GitHub Actions, previews Railway, tests unitaires et e2e). La supervision couvre les couches API, base de données et métier, tandis que le modèle économique repose sur des commissions transparentes via Stripe. Pour finaliser ce manuel, corriger les doublons et coquilles, stabiliser la mise en forme des tableaux, préciser versions et prérequis, et compléter les aspects sécurité/RGPD et secrets. Une fois ces ajustements appliqués, le document constitue une base solide et opérationnelle pour l’exploitation et l’évolution de l’application.</p>
  </div>

  <div class="card" id="architecture">
    <h2 class="section-title">Architecture & modules</h2>
    <ul class="list-tight">
      <li>Front/Back unifiés dans Next.js ; API routes sous <code>app/api/**</code></li>
      <li>Services métier : réservation, paiement, notifications, messagerie</li>
      <li>Prisma schema : modèles utilisateurs, posts, bookings, payments, notifications, etc.</li>
      <li>Stockage fichiers : S3 (upload) ; Cache/session : Redis</li>
    </ul>
  </div>

  <div class="card" id="flux">
    <h2 class="section-title">Flux de données</h2>
    <table role="grid" aria-label="Flux métier et données persistées">
      <thead>
        <tr>
          <th scope="col">Flux</th>
          <th scope="col">Étapes clés</th>
          <th scope="col">Données persistées</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>Auth</td>
          <td>Register → Email → Login → Session</td>
          <td>User, Session, VerificationToken</td>
        </tr>
        <tr>
          <td>Réservation</td>
          <td>Disponibilités → Booking → Confirmation</td>
          <td>Appointment, Booking, StatusHistory</td>
        </tr>
        <tr>
          <td>Paiement</td>
          <td>Checkout → Webhook → Sync DB</td>
          <td>Payment, Invoice, Commission</td>
        </tr>
        <tr>
          <td>Messagerie</td>
          <td>Conversation → Message → Realtime</td>
          <td>Conversation, Message, Notifications</td>
        </tr>
      </tbody>
    </table>
  </div>

  <div class="card" id="api">
    <h2 class="section-title">API principales</h2>
    <table role="grid" aria-label="Endpoints API par domaine">
      <thead>
        <tr>
          <th scope="col">Domaine</th>
          <th scope="col">Endpoints</th>
          <th scope="col">Objectif</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>Auth</td>
          <td><code>/api/auth/*</code></td>
          <td>Login, register, reset, verify</td>
        </tr>
        <tr>
          <td>Bookings</td>
          <td><code>/api/appointments/*</code></td>
          <td>Création, statut, transitions</td>
        </tr>
        <tr>
          <td>Paiements</td>
          <td><code>/api/payments/*</code></td>
          <td>Checkout, confirm, payout, refund</td>
        </tr>
        <tr>
          <td>Notifications</td>
          <td><code>/api/notifications</code>, <code>/api/push-subscriptions/*</code></td>
          <td>Envoi et préférences</td>
        </tr>
        <tr>
          <td>Messages</td>
          <td><code>/api/conversations/*</code></td>
          <td>Messagerie et realtime</td>
        </tr>
        <tr>
          <td>Recherche</td>
          <td><code>/api/search/*</code></td>
          <td>Posts, utilisateurs, tendances</td>
        </tr>
      </tbody>
    </table>
  </div>

  <div class="card" id="roles">
    <h2 class="section-title">Rôles & permissions</h2>
    <ul class="list-tight">
      <li>Client : recherche, réservation, paiement, messagerie, gestion profil.</li>
      <li>Artiste/Pro : gestion disponibilités, validation/annulation, stats, contenu.</li>
      <li>Admin : modération, gestion utilisateurs, accès aux dashboards.</li>
      <li>Contrôle d’accès : middleware + vérification des rôles sur routes sensibles.</li>
    </ul>
  </div>

  <div class="card" id="parcours">
    <h2 class="section-title">Parcours fonctionnels clés</h2>
    <ul class="list-tight">
      <li>Inscription → vérification email → login → onboarding.</li>
      <li>Recherche d’un artiste → réservation → paiement Stripe → confirmation.</li>
      <li>Messagerie : ouverture conversation → envoi → réception temps réel.</li>
      <li>Upload média : sélection fichier → upload S3 → affichage.</li>
    </ul>
  </div>

  <div class="card" id="exigences">
    <h2 class="section-title">Exigences techniques & fonctionnelles (CO2.5)</h2>
    <ul class="list-tight">
      <li>Performance : pages principales &lt; 3s en conditions normales, pagination/filtrage.</li>
      <li>Sécurité : validation entrée, contrôle d’accès, secrets en env.</li>
      <li>Compatibilité : web + mobile responsive, navigateurs modernes.</li>
      <li>Traçabilité : logs applicatifs + métriques (Prometheus/Grafana).</li>
    </ul>
  </div>

  <div class="card" id="donnees">
    <h2 class="section-title">Données & intégrité</h2>
    <ul class="list-tight">
      <li>Modèles clés : User, Post, Booking, Payment, Notification, Message.</li>
      <li>Contraintes : unicité email/username, relations booking↔paiement.</li>
      <li>Historisation : statuts de réservation et paiements conservés.</li>
      <li>RGPD : suppression/anonymisation des comptes si requis.</li>
    </ul>
  </div>

  <div class="card" id="configuration">
    <h2 class="section-title">Configuration & scripts</h2>
    <ul class="list-tight">
      <li>Variables env validées via <code>npm run env:check</code>.</li>
      <li>DB : <code>npm run db:generate</code>, <code>npm run db:push</code>, <code>npm run db:seed</code>.</li>
      <li>Notifications : <code>notifications:test</code> / <code>notifications:send-test</code>.</li>
      <li>Docs API : <code>api:docs:generate</code> / <code>api:docs:export</code>.</li>
    </ul>
  </div>

  <div class="card" id="tests">
    <h2 class="section-title">Tests & couverture (CO2.4)</h2>
    <p class="pill badge-success">État actuel : Jest (ts-jest) opérationnel — <code>npm test</code> et <code>npm run test:coverage</code> fonctionnent.</p>
    <ul class="list-tight">
      <li>Harnais ciblé : unitaires (Jest + ts-jest) + E2E (Playwright) + lint.</li>
      <li>Suites unitaires : <code>__tests__/api/*.test.ts</code>, <code>__tests__/components/*.test.tsx</code>, <code>__tests__/lib/*.test.ts</code>.</li>
      <li>Suites E2E : <code>e2e/auth-flow</code>, <code>booking-flow</code>, <code>payment-flow</code>, <code>basic-navigation</code>, <code>public-features</code>.</li>
      <li>Résultat : <code>npm test</code> exécute les tests unitaires ; <code>npm run test:coverage</code> génère le rapport lcov (plus d’erreur SWC — transpilation via ts-jest).</li>
      <li>Rapport de couverture : <code>coverage/lcov-report/index.html</code>.</li>
      <li>Modules critiques visés : Auth, Réservations, Paiement Stripe (commission), Notifications.</li>
      <li>Prévention régressions : scénarios E2E alignés sur les parcours (auth, réservation, paiement, upload, messaging).</li>
    </ul>
  </div>

  <div class="card" id="securite">
    <h2 class="section-title">Sécurité (C2.2.3 — OWASP Top 10)</h2>
    <div class="callout callout-info">
      <strong>Référentiel appliqué :</strong> OWASP Top 10 (2021) — les mesures ci-dessous couvrent les 10 failles de sécurité principales décrites par l’OWASP, conformément au critère C2.2.3 du référentiel YNOV 2024.
    </div>
    <table role="grid" aria-label="Couverture OWASP Top 10">
      <thead>
        <tr>
          <th scope="col">Faille OWASP</th>
          <th scope="col">Mesure implémentée dans INKSPOT</th>
          <th scope="col">Statut</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><strong>A01 — Broken Access Control</strong></td>
          <td>Contrôle d’accès par rôles (USER / PRO / ADMIN) via NextAuth ; middleware de vérification sur toutes les routes API sensibles</td>
          <td><span class="pill badge-success">Couvert</span></td>
        </tr>
        <tr>
          <td><strong>A02 — Cryptographic Failures</strong></td>
          <td>Mots de passe hashés (bcryptjs) ; HTTPS via Nginx ; secrets stockés en variables d’env Railway (jamais en clair) ; rotation recommandée</td>
          <td><span class="pill badge-success">Couvert</span></td>
        </tr>
        <tr>
          <td><strong>A03 — Injection</strong></td>
          <td>Requêtes DB via Prisma ORM (paramétrage automatique, pas de SQL brut) ; validation d’entrée avec schémas Zod sur toutes les routes sensibles</td>
          <td><span class="pill badge-success">Couvert</span></td>
        </tr>
        <tr>
          <td><strong>A04 — Insecure Design</strong></td>
          <td>Architecture modulaire (app/api, services, prisma) ; séparation des responsabilités ; revue de code via PR GitHub</td>
          <td><span class="pill badge-success">Couvert</span></td>
        </tr>
        <tr>
          <td><strong>A05 — Security Misconfiguration</strong></td>
          <td>Variables d’env Railway (pas de .env en production) ; headers HTTP sécurisés via Nginx ; ACL S3 restrictives</td>
          <td><span class="pill badge-success">Couvert</span></td>
        </tr>
        <tr>
          <td><strong>A06 — Vulnerable Components</strong></td>
          <td><code>npm audit</code> intégré au pipeline CI/CD ; script <code>security:scan</code> ; surveillance des dépendances (Dependabot recommandé)</td>
          <td><span class="pill badge-success">Couvert</span></td>
        </tr>
        <tr>
          <td><strong>A07 — Auth Failures</strong></td>
          <td>Sessions NextAuth (JWT + cookies httpOnly) ; rate-limit sur endpoints d’authentification ; verrouillage après tentatives échouées</td>
          <td><span class="pill badge-success">Couvert</span></td>
        </tr>
        <tr>
          <td><strong>A08 — Software Integrity Failures</strong></td>
          <td>Webhooks Stripe : vérification de signature HMAC + idempotence (prévention des replays) ; pipeline CI/CD avec vérification des artefacts</td>
          <td><span class="pill badge-success">Couvert</span></td>
        </tr>
        <tr>
          <td><strong>A09 — Logging Failures</strong></td>
          <td>Journalisation des événements critiques (auth, paiements, erreurs 5xx) ; logs Nginx + applicatifs ; Prometheus/Grafana pour les métriques</td>
          <td><span class="pill badge-success">Couvert</span></td>
        </tr>
        <tr>
          <td><strong>A10 — SSRF</strong></td>
          <td>Validation des types et tailles de fichiers uploadés ; ACL S3 + restriction des URLs autorisées pour les webhooks entrants</td>
          <td><span class="pill badge-success">Couvert</span></td>
        </tr>
      </tbody>
    </table>
  </div>

  <div class="card" id="accessibilite">
    <h2 class="section-title">Accessibilité (CO2.5 — RGAA 4.1 / WCAG 2.1)</h2>
    <div class="callout callout-info">
      <strong>Référentiel choisi :</strong> RGAA 4.1 (Référentiel Général d’Amélioration de l’Accessibilité), aligné sur WCAG 2.1 niveau AA — conformément au critère C2.2.3 du référentiel YNOV 2024 qui exige un « référentiel d’accessibilité choisi, présenté et justifié ». Le RGAA est le référentiel officiel français, applicable aux services numériques professionnels.
    </div>
    <table role="grid" aria-label="Checklist accessibilité RGAA / WCAG 2.1">
      <thead>
        <tr>
          <th scope="col">Critère RGAA</th>
          <th scope="col">Mesure implémentée</th>
          <th scope="col">Statut</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><strong>Images (1.x)</strong></td>
          <td>Attributs <code>alt</code> sur toutes les images significatives ; images décoratives avec <code>alt=""</code></td>
          <td><span class="pill badge-success">Implémenté</span></td>
        </tr>
        <tr>
          <td><strong>Couleurs (3.x)</strong></td>
          <td>Contraste AA (ratio ≥ 4,5:1 pour texte normal, ≥ 3:1 pour grand texte) ; information non transmise par la couleur seule</td>
          <td><span class="pill badge-neutral">À auditer</span></td>
        </tr>
        <tr>
          <td><strong>Tableaux (5.x)</strong></td>
          <td>Attributs <code>scope</code>, <code>role="grid"</code>, <code>aria-label</code> sur tous les tableaux de données</td>
          <td><span class="pill badge-success">Implémenté</span></td>
        </tr>
        <tr>
          <td><strong>Liens (6.x)</strong></td>
          <td>Intitulés de liens explicites ; pas de « cliquez ici » sans contexte</td>
          <td><span class="pill badge-success">Implémenté</span></td>
        </tr>
        <tr>
          <td><strong>Scripts (7.x)</strong></td>
          <td>Composants interactifs avec rôles ARIA explicites ; événements clavier sur tous les éléments cliquables</td>
          <td><span class="pill badge-success">Implémenté</span></td>
        </tr>
        <tr>
          <td><strong>Navigation (12.x)</strong></td>
          <td>Focus visible ; navigation clavier complète ; modales fermables au clavier (Escape) ; ordre de tabulation logique</td>
          <td><span class="pill badge-success">Implémenté</span></td>
        </tr>
        <tr>
          <td><strong>Formulaires (11.x)</strong></td>
          <td>Labels associés à chaque champ ; messages d’erreur annoncés via <code>aria-live</code> ; champs obligatoires marqués</td>
          <td><span class="pill badge-success">Implémenté</span></td>
        </tr>
      </tbody>
    </table>
    <h3>Outillage et audit</h3>
    <ul class="list-tight">
      <li><strong>Lint statique</strong> : <code>eslint-plugin-jsx-a11y</code> intégré au pipeline CI/CD</li>
      <li><strong>Audit automatisé</strong> : <code>axe-cli</code> et Playwright a11y sur les pages critiques (login, booking, paiement, messagerie)</li>
      <li><strong>Audit Lighthouse</strong> : score accessibilité cible ≥ 90/100</li>
      <li><strong>Statut</strong> : audit axe/Lighthouse planifié — voir anomalie REC-003 dans le <a href="/cahier-recette#anomalies">Cahier de Recette</a></li>
    </ul>
  </div>

  <div class="card" id="evolutivite">
    <h2 class="section-title">Évolutivité & maintenabilité</h2>
    <ul class="list-tight">
      <li>Migrations Prisma : <code>npm run db:migrate</code> / <code>db:push</code>, versionning clair</li>
      <li>Cache/Redis pour sessions et optimisation requêtes fréquentes</li>
      <li>Modules séparés (app/api, services, prisma) pour limiter le couplage</li>
      <li>Surveillance des dépendances (npm audit, security:scan)</li>
    </ul>
  </div>

  <div class="card" id="monitoring">
    <h2 class="section-title">Monitoring / Healthchecks</h2>
    <ul class="list-tight">
      <li>Endpoints santé : <code>/api/health</code> et variantes simples</li>
      <li>Prometheus + Grafana : métriques app/DB/cache</li>
      <li>Logs : app, nginx, Stripe webhooks</li>
    </ul>
  </div>

  <div class="card" id="exploitation">
    <h2 class="section-title">Exploitation & runbook</h2>
    <ul class="list-tight">
      <li>Surveiller erreurs 5xx et latence des endpoints critiques.</li>
      <li>Vérifier quotidiennement les jobs Stripe/webhooks.</li>
      <li>Planifier les migrations Prisma hors pic de charge.</li>
      <li>Backups : base de données + sauvegarde des médias S3.</li>
    </ul>
  </div>

  <div class="card" id="depannage">
    <h2 class="section-title">Dépannage rapide</h2>
    <ul class="list-tight">
      <li>Login KO : vérifier <code>NEXTAUTH_URL</code> + secrets.</li>
      <li>Paiement KO : vérifier clés Stripe + webhook secret.</li>
      <li>Upload KO : vérifier credentials S3 + ACL.</li>
      <li>E2E KO : vérifier port webServer + config Playwright.</li>
    </ul>
  </div>

  <div class="card" id="annexes">
    <h2 class="section-title">Annexes</h2>
    <p>Annexes du manuel (A à H) :</p>
    <table role="grid" aria-label="Annexes du manuel">
      <thead><tr><th scope="col">Annexe</th><th scope="col">Contenu</th></tr></thead>
      <tbody>
        <tr><td>A</td><td>Architecture de la stack technique (schéma)</td></tr>
        <tr><td>B</td><td>Flux d’authentification et données</td></tr>
        <tr><td>C</td><td>Architecture de monitoring (vue synthétique)</td></tr>
        <tr><td>D</td><td>Gestion des dépendances (processus)</td></tr>
        <tr><td>E</td><td>Supervision détaillée (métriques, séries temporelles)</td></tr>
        <tr><td>F</td><td>Flux CI/CD et déploiement</td></tr>
        <tr><td>G</td><td>Paiements &amp; commissions (Stripe)</td></tr>
        <tr><td>H</td><td>Fonctionnalités &amp; workflows clés</td></tr>
      </tbody>
    </table>
    <p><strong>Ressources projet</strong></p>
    <ul class="list-tight">
      <li>Routes API : inventaire généré depuis <code>app/api/**</code> (≈ 80 routes listées)</li>
      <li>Modèles Prisma : <code>prisma/schema.prisma</code> (User, Post, Conversation, Message, Appointment, Booking, Payment, Invoice, Review, Notification, etc.)</li>
      <li>Docs Swagger : scripts <code>api:docs:generate</code> / <code>api:docs:export</code> (swagger-jsdoc)</li>
    </ul>
  </div>

  <div class="card" id="mise-a-jour">
    <h2 class="section-title">Manuel de mise à jour (C2.4.1)</h2>
    <div class="callout callout-info">
      <strong>Référentiel :</strong> Ce chapitre répond au critère C2.4.1 du référentiel YNOV 2024 — « Rédiger la documentation technique : manuel de mise à jour ». Il décrit la procédure de mise à jour des dépendances, des versions applicatives et des migrations de base de données.
    </div>
    <h3>Mise à jour des dépendances</h3>
    <table role="grid" aria-label="Procédure de mise à jour des dépendances">
      <thead>
        <tr>
          <th scope="col">Étape</th>
          <th scope="col">Commande</th>
          <th scope="col">Fréquence</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>Audit de sécurité</td>
          <td><code>npm audit</code></td>
          <td>Hebdomadaire / après chaque PR</td>
        </tr>
        <tr>
          <td>Correction automatique des vulnérabilités</td>
          <td><code>npm audit fix</code></td>
          <td>Dès qu’une vulnérabilité est détectée</td>
        </tr>
        <tr>
          <td>Vérification des mises à jour disponibles</td>
          <td><code>npm outdated</code></td>
          <td>Mensuelle</td>
        </tr>
        <tr>
          <td>Mise à jour d’une dépendance spécifique</td>
          <td><code>npm update [package]</code></td>
          <td>Selon besoin / correctif de sécurité</td>
        </tr>
        <tr>
          <td>Mise à jour majeure (breaking changes)</td>
          <td><code>npm install [package]@latest</code> + tests complets</td>
          <td>Planifiée (sprint dédié)</td>
        </tr>
      </tbody>
    </table>
    <h3>Mise à jour de l’application</h3>
    <ul class="list-tight">
      <li><strong>Type automatique (Railway)</strong> : tout push sur la branche <code>dev</code> déclenche un redéploiement automatique via Railway CI/CD.</li>
      <li><strong>Type manuel</strong> : via le dashboard Railway → « Deploy » → sélectionner le commit cible.</li>
      <li><strong>Rollback</strong> : Railway conserve l’historique des déploiements ; rollback en 1 clic vers une version précédente.</li>
    </ul>
    <h3>Migrations de base de données</h3>
    <ul class="list-tight">
      <li><strong>Créer une migration</strong> : <code>npx prisma migrate dev --name [nom-migration]</code> (environnement de développement)</li>
      <li><strong>Appliquer en production</strong> : <code>npx prisma migrate deploy</code> (exécuté automatiquement au démarrage via Railway)</li>
      <li><strong>Vérifier l’état</strong> : <code>npx prisma migrate status</code></li>
      <li><strong>Sauvegarde avant migration</strong> : toujours effectuer un backup DB avant une migration en production (voir Manuel de Déploiement).</li>
    </ul>
    <h3>Gestion des versions (SemVer)</h3>
    <table role="grid" aria-label="Convention de versioning sémantique">
      <thead>
        <tr>
          <th scope="col">Type de changement</th>
          <th scope="col">Incrément de version</th>
          <th scope="col">Exemple</th>
        </tr>
      </thead>
      <tbody>
        <tr><td>Correctif de bug (patch)</td><td>PATCH (+0.0.1)</td><td>1.0.0 → 1.0.1</td></tr>
        <tr><td>Nouvelle fonctionnalité rétrocompatible</td><td>MINOR (+0.1.0)</td><td>1.0.0 → 1.1.0</td></tr>
        <tr><td>Changement incompatible (breaking)</td><td>MAJOR (+1.0.0)</td><td>1.0.0 → 2.0.0</td></tr>
      </tbody>
    </table>
    <p>L’historique complet des versions est tracé dans le <a href="/changelog">Changelog</a> (critère C4.3.2 — Journal des versions).</p>
  </div>

  <div class="card" id="glossaire-mut">
    <h2 class="section-title">Glossaire</h2>
    <table role="grid" aria-label="Définitions des termes techniques">
      <thead><tr><th scope="col">Terme</th><th scope="col">Définition</th></tr></thead>
      <tbody>
        <tr><td>CI/CD</td><td>Intégration et déploiement continus (GitHub Actions + Railway)</td></tr>
        <tr><td>SCA / 3‑D Secure</td><td>Strong Customer Authentication : authentification forte pour les paiements en ligne</td></tr>
        <tr><td>Idempotence</td><td>Traitement d’un webhook ou d’une requête sans effet de bord en cas de doublon</td></tr>
        <tr><td>Preview</td><td>Environnement Railway temporaire créé à partir d’une Pull Request</td></tr>
        <tr><td>Runbook</td><td>Procédure opérationnelle pour l’exploitation et le dépannage</td></tr>
        <tr><td>Healthcheck</td><td>Endpoint ou script vérifiant la disponibilité de l’application ou des services</td></tr>
      </tbody>
    </table>
  </div>

  <div class="card" id="references-croisees-mut">
    <h2 class="section-title">Références croisées</h2>
    <p>Documents et pages complémentaires de la documentation INKSPOT :</p>
    <ul class="list-tight">
      <li><strong><a href="/cahier-recette">Cahier de Recette</a></strong> — Plan de tests, scénarios fonctionnels, critères d’acceptation, E2E et couverture (référentiel CO2.7).</li>
      <li><strong><a href="/deploiement-railway">Manuel de Déploiement Railway</a></strong> — Prérequis, variables d’environnement complètes, build, migrations, CI/CD, observabilité, sauvegardes et incidents.</li>
      <li><strong><a href="/changelog">Changelog</a></strong> — Historique des évolutions et corrections du projet.</li>
    </ul>
    <p>Documentation technique du dépôt : <code>docs/</code> (CI/CD, environnement, déploiement Railway, dépannage VAPID, etc.).</p>
  </div>

  <div class="card" id="table-sections-mut">
    <h2 class="section-title">Table des sections</h2>
    <p>Index des sections de ce manuel :</p>
    <table role="grid" aria-label="Sections du manuel">
      <thead>
        <tr>
          <th scope="col">Section</th>
          <th scope="col">Présent</th>
          <th scope="col">Emplacement</th>
        </tr>
      </thead>
      <tbody>
        <tr><td>Informations du document</td><td>Oui</td><td><a href="#infos-document">#infos-document</a></td></tr>
        <tr><td>1. Introduction (Présentation, Vision, Pour qui, Approche, Philosophie, Accessibilité)</td><td>Oui</td><td><a href="#introduction">#introduction</a></td></tr>
        <tr><td>2. Stack technique (tableau + annexe A)</td><td>Oui</td><td><a href="#stack">#stack</a> + Annexes</td></tr>
        <tr><td>3. Dépendances — 3.a Scripts</td><td>Oui</td><td><a href="#dependances">#dependances</a></td></tr>
        <tr><td>3. Dépendances — 3.b Dépendances principales</td><td>Oui</td><td>Même section</td></tr>
        <tr><td>3. Dépendances — 3.c Procédure de gestion</td><td>Oui</td><td>Même section</td></tr>
        <tr><td>4. Système de supervision (Vue d’ensemble, Métriques, Indicateurs, Statuts, Priorités)</td><td>Oui</td><td><a href="#supervision">#supervision</a></td></tr>
        <tr><td>5. Maintenance — 5.a Déploiement continu</td><td>Oui</td><td><a href="#maintenance">#maintenance</a></td></tr>
        <tr><td>5. Maintenance — 5.b Méthodologie de test</td><td>Oui</td><td>Même section</td></tr>
        <tr><td>5. Maintenance — 5.c Méthodologie GitHub</td><td>Oui</td><td>Même section</td></tr>
        <tr><td>6. Modèle économique — 6.a, 6.b, 6.c</td><td>Oui</td><td><a href="#modele-economique">#modele-economique</a></td></tr>
        <tr><td>Conclusion</td><td>Oui</td><td>Même section</td></tr>
        <tr><td>Annexes A à H (architecture, flux auth, monitoring, dépendances, CI/CD, paiements, workflows)</td><td>Oui</td><td><a href="#annexes">#annexes</a> + sections techniques</td></tr>
      </tbody>
    </table>
  </div>

  <div class="card" id="reference-mut">
    <h2 class="section-title">Référence document</h2>
    <p><strong>Manuel d’utilisation technique INKSPOT.</strong> Ce document constitue le livrable officiel du manuel d’utilisation technique.</p>
    <p><strong>Validation Master</strong> : ce manuel est conforme aux attentes du livrable « Manuel d’utilisation technique » du référentiel YNOV 2024 (critères CO2.4 et CO2.5). Les sections Tests &amp; couverture, Exigences techniques, Sécurité, Accessibilité et Évolutivité sont détaillées et tracées ; la conformité est explicitée dans la section <a href="#conformite-referentiel-mut">Conformité référentiel</a>.</p>
  </div>
</DocLayout>
